# Container-Skripte müssen im Working Tree LF behalten, sonst scheitert der
# Shebang im Linux-Image ("bad interpreter: no such file or directory").
*.sh              text eol=lf
*.conf            text eol=lf
Dockerfile        text eol=lf
.dockerignore     text eol=lf
git-credential-*  text eol=lf
